# Default run configuration. Any value can be overridden from the CLI:
#   mvn test -Dbrowser=firefox -Dheadless=true
browser=chrome
headless=false
base.url=https://www.saucedemo.com
implicit.wait.seconds=5
explicit.wait.seconds=15
